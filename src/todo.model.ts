export interface Todo {
  doctor_id: string;
  first_name: string;
  last_name: string;
  degree: string;
  dob: string;
}

export interface Task {
  task_id: string;
  owner: string;
  priority: string;
}